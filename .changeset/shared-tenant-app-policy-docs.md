---
"@agent-native/core": patch
---

Docs: clarify that a mix of shared and tenant-specific apps needs app-level policy on top of agent-native's existing workspace, org, sharing, and action primitives — not a new architecture. Adds a "Shared apps, tenant-specific apps, and entitlements" section plus a framework-vs-policy-vs-separate-architecture recommendation to the multi-app workspace guide, and aligns the authentication, database, actions, server, content, and workspace-connections docs (and the authentication skill).
